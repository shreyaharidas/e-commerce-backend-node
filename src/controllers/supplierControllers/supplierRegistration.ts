import { Request, Response, Router } from 'express';

const supplierRegistration = (req: Request, res: Response): void => {
    // Your TypeScript code for supplierRegistration goes here
  };
  
  export { supplierRegistration };
  